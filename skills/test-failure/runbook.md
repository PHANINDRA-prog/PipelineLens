# Test failures

Use the failed test name, assertion, and command as the initial evidence set. Determine whether the failure is deterministic, environment-specific, or caused by a downstream dependency. Preserve the test as a regression check while repairing the behavior.
