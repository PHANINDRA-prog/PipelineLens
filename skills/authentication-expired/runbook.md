# Credential failures

Use this skill only when the log contains a direct authentication signal, such as HTTP 401 or an explicit expiration message. Verify the credential in the provider's protected secret store, preserve least privilege, and rerun only after the approved rotation process is complete.
