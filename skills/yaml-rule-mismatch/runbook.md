# Rule mismatch

Compare the pipeline event, ref, changed files, and variables to the exact rule responsible for selecting the job. A skipped job can be correct behavior; only change the rule when the intended release policy is explicit.
